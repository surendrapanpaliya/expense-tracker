# CLAUDE.md — expense-tracker

This lab carries the athena SDD overlay in `athena-sdd-overlay/`. The overlay's `AGENTS.md` is the canonical agent guide for any SDD-related work in this repo. Import it below.

@athena-sdd-overlay/AGENTS.md

## Product context

The product brief for the expense-tracker app lives at [`./spec.md`](./spec.md). It is language-agnostic. Pick Java, Node, or Python for the implementation.

If you're working on the spec lifecycle (read a spec, plan against it, validate it), the SDD overlay's contract is what governs. If you're implementing the app itself, the product brief at `spec.md` is the source.
